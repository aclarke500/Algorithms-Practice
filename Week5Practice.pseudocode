// Counting Occurrences: Given a list s of sorted integers, where there are duplicates, 
// count the
// occurrences of a given number k.
// Examples: Input ([1,1,1,3,3,3,5], 3) returns 3
// Input ([6,6,6,8,8,10,10,10,10], 8) returns 2
// A) Provide clear pseudocode for your algorithm
function countOccurences(s, k) 
begin

numeric n = length(s) // 2 (lower bound)
numeric jumps = 0;
while s[n] != k
begin
  if s[n] > k


end
